//! npm registry feed and fetch (stub for Phase 3).
//!
//! Full implementation of the npm replication changes stream
//! (`_changes` feed) and registry metadata resolution will be
//! added in Phase 3. This module exists so the `NpmRegistry` type
//! is wired and the ecosystem dispatch compiles.
